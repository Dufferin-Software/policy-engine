apiVersion: 1
providers:
  - name: policy-engine
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /usr/share/policy-engine/grafana
