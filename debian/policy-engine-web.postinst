#!/bin/sh
set -e

#DEBHELPER#

# Restart the engine so it picks up the newly installed web UI files.
if [ "$1" = "configure" ] && [ -d /run/systemd/system ]; then
    if systemctl is-active --quiet policy-engine.service; then
        systemctl restart policy-engine.service || true
    fi
fi
